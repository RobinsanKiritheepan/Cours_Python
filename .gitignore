# Fichiers temporaires Python / Jupyter
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Fichiers systeme
.DS_Store
Thumbs.db
desktop.ini
__MACOSX/
# verrous temporaires Office (crees quand un .pptx/.docx est ouvert)
~$*

# Environnements virtuels
venv/
.venv/
env/
ENV/

# Sauvegardes locales (copies de travail, pas leur place sur le depot)
*.backup_avant_gpu
*.backup

# Sorties d'entrainement PyTorch Lightning (CSVLogger, checkpoints).
# Regenerees a chaque trainer.fit() : un nouveau version_N a chaque run.
# C'est du resultat de calcul, pas du code -> ca n'a pas sa place sur le depot.
lightning_logs/

# Le mini-projet de segmentation a son PROPRE depot git.
# Il embarque un dataset de plusieurs Go (PennFudanPed / Supervisely) : il n'a
# rien a faire ici. Un "git add -A" a la racine l'aspirait entierement (13 355
# fichiers, 12 Go) -- d'ou cette regle.
mini-projet/
mini-projet.zip

# NOTE : les CSV de donnees/ NE SONT PAS ignores, et c'est voulu.
# Les notebooks Colab les lisent par leur URL GitHub brute :
# https://raw.githubusercontent.com/RobinsanKiritheepan/Cours_Python/main/<chemin>
# Les ignorer casserait les notebooks (404). Ils doivent etre pousses.
